from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Developing a machine learning-based model for predicting the optimal airfare price at a given time slot.',
    author='Raghunathan R',
    license='',
)
